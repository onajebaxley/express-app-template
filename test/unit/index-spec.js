'use strict';

const chai = require('chai');
const expect = chai.expect;

describe('_index', () => {
    it('should expose the desired public properties and methods', () => {
        expect(true).to.be.true;
    });
});
